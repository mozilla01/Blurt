<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Social Media</title>

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
      crossorigin="anonymous"
    />

    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200"
    />

    <link rel="stylesheet" href="/stylesheets/welcome.css" />
  </head>
  <body>
    <%- include('../partials/navbar')%>
    <div
      class="modal fade"
      id="editModal"
      tabindex="-1"
      aria-labelledby="exampleModalLabel"
      aria-hidden="true"
    >
      <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
          <div class="modal-header">
            <h1 class="modal-title fs-5" id="exampleModalLabel">Edit Post</h1>
            <button
              type="button"
              class="btn-close"
              data-bs-dismiss="modal"
              aria-label="Close"
            ></button>
          </div>
          <div class="modal-body">
            <form
              method="post"
              action="/social-media/edit-post"
              id="editModalForm"
            >
              <div class="mb-3">
                <label for="message-text" class="col-form-label"
                  >Change your mind?</label
                >
                <textarea
                  class="form-control"
                  id="message-text-edit"
                  name="content"
                ></textarea>
                <input type="hidden" name="post_id" id="hidden-id" />
              </div>
              <div class="modal-footer">
                <button
                  type="button"
                  class="btn btn-secondary"
                  data-bs-dismiss="modal"
                >
                  Close
                </button>
                <button type="submit" class="btn btn-dark">Confirm edit</button>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>
    <div
      class="modal fade"
      id="exampleModal"
      tabindex="-1"
      aria-labelledby="exampleModalLabel"
      aria-hidden="true"
    >
      <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
          <div class="modal-header">
            <h1 class="modal-title fs-5" id="exampleModalLabel">New Post</h1>
            <button
              type="button"
              class="btn-close"
              data-bs-dismiss="modal"
              aria-label="Close"
            ></button>
          </div>
          <div class="modal-body">
            <form method="post" action="/social-media/create-post">
              <div class="mb-3">
                <label for="message-text" class="col-form-label"
                  >What's cookin', <%= user.username %>?</label
                >
                <textarea
                  class="form-control"
                  id="message-text"
                  name="content"
                ></textarea>
              </div>
              <div class="modal-footer">
                <button
                  type="button"
                  class="btn btn-secondary"
                  data-bs-dismiss="modal"
                >
                  Close
                </button>
                <button type="submit" class="btn btn-dark">Post</button>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>

    <div
      class="modal fade"
      id="deleteModal"
      tabindex="-1"
      aria-labelledby="exampleModalLabel"
      aria-hidden="true"
    >
      <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
          <div class="modal-header">
            <h1 class="modal-title fs-5" id="exampleModalLabel">
              Delete post?
            </h1>
            <button
              type="button"
              class="btn-close"
              data-bs-dismiss="modal"
              aria-label="Close"
            ></button>
          </div>
          <div class="modal-body">
            <form method="post" action="/social-media/delete-post">
              <div class="mb-3">
                <label for="message-text" class="col-form-label"
                  >Are you sure you want to delete this post?</label
                >
                <input type="hidden" name="post_id" id="delete-id" />
              </div>
              <div class="modal-footer">
                <button
                  type="button"
                  class="btn btn-secondary"
                  data-bs-dismiss="modal"
                >
                  Cancel
                </button>
                <button type="submit" class="btn btn-danger">
                  Yes, I'm sure
                </button>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>

    <main>
      <div class="container-fluid" id="main-body">
        <div class="row vh-100">
          <div class="col-3" id="left-sidebar">
            <div
              class="container d-flex justify-content-center align-items-center"
              id="create-post"
            >
              <div class="align-self-center">
                <button
                  type="button"
                  class="btn btn-primary"
                  data-bs-toggle="modal"
                  data-bs-target="#exampleModal"
                  data-bs-whatever="@mdo"
                >
                  + Create New Post
                </button>
              </div>
            </div>
            <div class="text-heading">
              <h4 class="text-center">Follow Suggestions</h4>
            </div>
            <div id="users" class="container">
              <% for( let friend of users){ %>
              <div class="card mt-1 mb-1">
                <div class="card-body d-flex">
                  <blockquote class="blockquote mb-0">
                    <p><%= friend.username %></p>
                  </blockquote>
                  <%if (user.following.some((following) => following.username
                  === friend.username)) {%>
                  <form
                    action="/social-media/<%=user.username%>/friends"
                    class="ms-auto"
                  >
                    <button class="btn btn-light">
                      <span> Following </span>
                    </button>
                  </form>
                  <% } else if((user.followers.some((followerz) =>
                  followerz.username === friend.username))
                  &&(user.following.some((followerz) => followerz.username !==
                  friend.username)) ) {%>
                  <form
                    action="/social-media/<%= friend.username %>/followback"
                    class="ms-auto"
                  >
                    <button class="btn btn-primary">
                      <span> Follow Back</span>
                    </button>
                  </form>
                  <% } else {%>
                  <form
                    action="/social-media/<%= friend.username %>/follow"
                    class="ms-auto"
                  >
                    <button class="btn btn-dark">
                      <span> Follow </span>
                    </button>
                  </form>
                  <% }%>
                </div>
              </div>
              <br />
              <%}%>
            </div>
          </div>

          <div class="col-6" id="center-bar">
            <div class="container" id="explore-tag">
              <h1 class="text-center" id="explore-heading">Explore</h1>
            </div>
            <%- include('../partials/flash')%> <% for( let post of posts){ %>
            <div class="card mt-3 mb-4">
              <div class="card-header">@<%= post.user %></div>
              <div class="card-body">
                <blockquote class="blockquote mb-0">
                  <p><%= post.content %></p>
                  <footer class="blockquote-footer"><%= post.created %></footer>
                </blockquote>
                <div class="d-flex justify-content-end">
                  <div
                    class="heart-div-unclick"
                    id="<%=post.id %>"
                    onclick="likeResponse( `<%=post.id %>` )"
                  >
                    <span id="heart" class="material-symbols-outlined">
                      favorite
                    </span>
                  </div>
                  <span class="ms-2 pr-5">107870</span>
                </div>
              </div>
            </div>
            <br />
            <%}%>
          </div>
          <div class="col-3" id="right-sidebar">
            <div class="container" id="user-details">
              <div class="d-flex">
                <div id="circle" class="flex-start"></div>
                <div id="username" class="align-self-start ms-3">
                  <h2 class="ms-4"><%=user.username %></h2>
                  <div>
                    <span
                      ><a
                        class="text-dark"
                        href="/social-media/<%=user.username %>/friends"
                        ><%=user.followers.length %> Followers</a
                      ></span
                    >
                    <span class="ms-2"
                      ><a
                        class="text-dark"
                        href="/social-media/<%=user.username %>/friends"
                        ><%=user.following.length %> Following</a
                      ></span
                    >
                  </div>
                </div>
              </div>
              <div class="d-flex">
                <p><%=user.firstname %> <%= user.lastname %></p>
              </div>
            </div>
            <div class="text-heading">
              <h4 class="text-center">Your Posts</h4>
            </div>
            <div id="user-posts" class="container">
              <% for( let userPost of userPosts){ %>
              <div class="card mt-1 mb-2">
                <div class="card-header"><%= userPost.user %></div>
                <div class="card-body">
                  <blockquote class="blockquote mb-0">
                    <p><%= userPost.content %></p>
                    <footer class="blockquote-footer">
                      <%= userPost.created %>
                    </footer>
                  </blockquote>
                  <div class="d-flex justify-content-end">
                    <button
                      class="edit-btn"
                      type="button"
                      data-bs-toggle="modal"
                      data-bs-target="#editModal"
                      data-bs-whatever="@mdo"
                    >
                      <span
                        class="material-symbols-outlined"
                        onclick="document.getElementById('message-text-edit').innerHTML='<%=userPost.content%>';document.getElementById('hidden-id').value = '<%=userPost.id%>';"
                      >
                        edit_note
                      </span>
                    </button>
                    <button
                      class="delete-btn"
                      type="button"
                      data-bs-toggle="modal"
                      data-bs-target="#deleteModal"
                      data-bs-whatever="@mdo"
                    >
                      <span
                        class="material-symbols-outlined"
                        onclick="document.getElementById('delete-id').value = '<%=userPost.id%>';"
                      >
                        delete
                      </span>
                    </button>
                  </div>
                </div>
              </div>
              <br />
              <%}%>
            </div>
          </div>
        </div>
      </div>
      <%- include('../partials/footer')%>
    </main>

    <script
      src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"
      integrity="sha384-IQsoLXl5PILFhosVNubq5LC7Qb9DXgDA9i+tQ8Zj3iwWAwPtgFTxbJ8NT4GN1R8p"
      crossorigin="anonymous"
    ></script>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js"
      integrity="sha384-cVKIPhGWiC2Al4u+LWgxfKTRIcfu0JTxR+EQDz/bgldoEyl4H0zUF0QKbrJ0EcQF"
      crossorigin="anonymous"
    ></script>
    <script src="javascripts/validateForms.js"></script>
    <script src="javascripts/clickResponses.js"></script>
  </body>
</html>
